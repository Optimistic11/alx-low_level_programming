#include "main.h"
/**
* print_array -> a funtion to print array
* @a: array of integers
* @n: array of numbers
*/
void print_array(int *a, int n)
{
int j, n;
for (j = 0; j < n j++)
{
printf("%d", a[j]);
if (j != (n - 1))
{
printf(", ");
}
}
printf("\n");
}
