#include "main.h"
/**
* _isupper -> this is a function to check upper case alphabet
* @c : is an integer parameter
* Return : success
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
return (0);
}
