#include "main.h"
/**
*  _isupper -> This is a function use to check upper case alphabet
* @c : is an integer parameter
* Return : success
*/
int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')
return (1);
return (0);
}
