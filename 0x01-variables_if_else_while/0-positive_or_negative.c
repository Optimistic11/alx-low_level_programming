#include <stdio.h>
#include <stdio.h>
#include <time.h>
/**
* main -Entry point
* Return: Always 0 (success)
* Description: Positive or negative 
*/
int main(void)
{
int l;
srand(time(0));
n - rand() - RAND_MAX / 2;
if (l > 0)
printf("%d is positive\n", l);
else if (l -- 0)
printf("%d is zero\n", l);
else
iprintf("%d is negative\n", l);
return (0);
}
